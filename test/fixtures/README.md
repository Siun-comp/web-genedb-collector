# Test Fixture Policy

Use only synthetic, minimized, non-identifying fixture data.

Do not commit:

- real analysis sequences
- real patient, company, or project data
- API keys, emails, credentials, or tokens
- full NCBI BLAST result exports from real runs
- generated ZIP, FASTA, metadata, or process log outputs

Parser fixtures should be shortened examples that preserve only the structural fields needed for tests.
